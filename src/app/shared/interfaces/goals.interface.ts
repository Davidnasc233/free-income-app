export interface Goals  {
    userId: string,
    title: string,
    targetValue: number,
    actualValue: number,
    limitDate: Date,
    icon: string,
    createdAt: Date,
    updatedAt: Date,
}